
  <div class="col-sm-2">
    <nav class="nav-sidebar">
		<ul class="nav tabs">
          <li class="active"><a href="#tab19" data-toggle="tab">Médicos por Unidade</a></li>
          <hr>
          <li class="">
            <% @work_unit.each do |work_unit| %>
              <% if work_unit.name == "DRS CANDANGOL./BAND/RIAC.FUNDO"%>
                <li class=""><a href="#tab<%= work_unit.id %>" data-toggle="tab">DRS CANDANGOL./ BAND/ RIAC.FUNDO</a></li>
              <% else %>
                <li class=""><a href="#tab<%= work_unit.id %>" data-toggle="tab"><%=work_unit.name%></a></li>
              <% end %>
              <hr>
            <% end %>
          </li>                            
		</ul>
	</nav>
</div>
<!-- tab content -->
<div class="tab-content col-sm-10">
<div class="tab-pane active text-style" id="tab19">
  <h2>Médicos por unidade</h2>
       <hr> 
       <%= render partial: "workunits_graph" %>  
</div>
<% @work_unit.each do |work_unit|%>
  <div class="tab-pane text-style" id="tab<%= work_unit.id %>">
    <h2>Quantidade de médicos em <%= work_unit.name %></h2
    <% @name = work_unit.name %>
    <% @array_speciality = array_speciality(work_unit.id) %>
      <% @array_quantity_medics = array_medics_quantity(work_unit.id, array_speciality(work_unit.id)) %>
     <%= render partial: "quantity_medics_by_speciality" %>  
      <hr>
  </div>
<%end%>
    

